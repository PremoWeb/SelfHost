---
description: Always use the --bun flag
---

When running any `bun` commands, specifically `bun run build` or `bun dev`, you MUST always use the `--bun` flag.

Example:
`bun --bun run build`
`bun --bun dev`
